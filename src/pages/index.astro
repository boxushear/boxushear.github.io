---

---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome | boxushear</title>
    <style>
      body {
        margin: 0;
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        background: radial-gradient(
          circle at 20% 30%,
          #ffffff,
          #f2f5fa,
          #e7ebf0
        );
        font-family: "Inter", "Noto Sans JP", sans-serif;
        color: #222;
        text-align: center;
      }

      h1 {
        font-size: 3rem;
        font-weight: 700;
        background: linear-gradient(90deg, #6a11cb, #2575fc);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 30px;
        animation: fadeIn 1s ease-in;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      p {
        color: #555;
        font-size: 1.1rem;
        margin-bottom: 40px;
        animation: fadeIn 1s ease-in 0.2s both;
      }

      .btn-group {
        display: flex;
        gap: 20px;
        animation: fadeIn 1s ease-in 0.4s both;
      }

      a {
        text-decoration: none;
        background: white;
        border: 1px solid #d0d7e2;
        border-radius: 25px;
        padding: 12px 36px;
        margin: 8px;
        color: #2575fc;
        font-weight: 600;
        font-size: 1.1rem;
        box-shadow: 0 2px 8px rgba(37, 117, 252, 0.15);
        transition: all 0.3s ease;
      }

      a:hover {
        background: linear-gradient(90deg, #6a11cb, #2575fc);
        color: white;
        transform: translateY(-4px);
        box-shadow: 0 6px 16px rgba(37, 117, 252, 0.25);
      }

      footer {
        position: absolute;
        bottom: 20px;
        font-size: 0.85rem;
        color: #777;
      }

      @media (max-width: 600px) {
        h1 {
          font-size: 2.2rem;
        }
        p {
          font-size: 1rem;
          margin-bottom: 28px;
        }
        .btn-group {
          flex-direction: column;
          gap: 14px;
        }
        a {
          padding: 10px 28px;
          font-size: 1rem;
        }
      }
    </style>
  </head>

  <body>
    <h1>Welcome / ようこそ — My Portfolio</h1>
    <p>Choose your language / 言語を選択してください</p>

    <div class="btn-group">
      <a href="/en">🌍 English</a>
      <a href="/ja">🇯🇵 日本語</a>
    </div>

    <footer>© 2025 boxushear | Powered by Astro 🚀</footer>
  </body>
</html>
